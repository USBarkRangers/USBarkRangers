# Native iOS rebuild execution record

Started September 13, 2026. Scope: Phase 1 of the approved [rebuild plan](../plans/IOS_NATIVE_BACKEND_REBUILD_PLAN_2026-09-13.md). Phase 1 is **in progress**, not accepted. As of September 14 the isolated native backend is deployed and version 0.5.16 (75) is installed on the owner's iPhone; owner email verification/test-access and physical trip/offline acceptance remain pending. The [current cloud acceptance record](IOS_NATIVE_CLOUD_ACCEPTANCE.md) supersedes historical undeployed/configuration-pending statements below. Follow the [recovery prompts](../plans/IOS_NATIVE_REBUILD_RECOVERY_PROMPTS_2026-09-13.md) for remaining work; local passes alone do not establish readiness.

## Boundaries

- Preserve the existing dirty working tree; the initial tree already contains extensive iOS/backend changes and untracked files.
- Do not deploy, migrate, clean up or disable any existing production project. All new deployment targets require explicit allowlisting.
- Keep current UI, leaderboard count and product limits. Future journal/dogs/media receive boundary comments, not feature scaffolding.
- Fix ownership, typed data flow and bounded storage at their source. File splitting by itself is not acceptance evidence.
- No public App Store release. Owner acceptance follows Phase 2.

## Setup decisions / external gates

- Owner approved and project created: `bark-ranger-ios` (display name: Bark Ranger iOS, project number `360077919845`). Firebase reported ready and the CLI verified ACTIVE. Optional Gemini and Analytics are disabled.
- Separate exact-target native deployment configuration and guard added; existing production configuration/default alias unchanged. Firestore rules are initially deny-all, not a completed application authorization contract.
- Owner approved `us-east1`. Firestore Standard `(default)` created there, with deny-all production-mode rules. CLI subsequently enabled deletion protection successfully.
- Registered bundle `swarm.USBARKRANGERS`, Firebase app `1:360077919845:ios:cd94b1ea6899f95da6e88c`. Downloaded owner configuration into ignored `06-config/native-ios/GoogleService-Info.plist`; not installed into the app yet.
- Owner approved Blaze, a $25 monthly alert and a $10 development/load-test ceiling. The initial link failed on Google's linked-project quota. The owner subsequently disabled billing for unused “My First Project” (`project-a47f323e-70da-41d8-be0`) themselves; the console confirmed this. Junior Ranger, existing Bark and JDDM remain linked and unchanged. This one owner action is not authorization for further non-target changes.
- Retried native billing link successfully on September 13; Firebase confirmed Blaze. Created alerts-only budget `604568be-fad5-41ff-aa1a-79e4d4258a35`, “Bark Ranger iOS — $25 monthly,” scoped to `bark-ranger-ios`, all services, with actual-cost thresholds at $5, $10 and $25 and billing-admin/user email notifications. Verified the saved budget row, $0.00 reported spend. This is not a hard cap and delayed reporting is not proof of zero incurred cost. Development/load-test ceiling remains $10; no large cloud load run authorized.
- Gemini and optional Google Analytics remain off. Paid billing, irreversible data location, Apple agreements/products and material spending choices require explicit owner approval.
- Local Firebase CLI is available through the repository's installed `firebase-tools`; Xcode and Swift are installed.
- Owner confirmed Apple Developer enrollment is not approved and explicitly requested disabled/commented activation points. Added `APPLE-ACTIVATION` comments at signing, account construction, membership actions and server access boundaries. Apple sign-in remains false, its entitlement key remains commented out, and no purchase handler or Premium bypass is enabled. StoreKit sketches identify intended wiring; the service/verification implementation is **not complete**. Approval must be followed by real team/provider/product configuration and device/sandbox verification before activation.
- Owner subsequently clarified: implement the real Apple service/backend in Phase 1, with only approval-dependent activation disabled. The earlier sketches do not satisfy this requirement. Continue implementation; do not stop Phase 1 merely because enrollment is pending.

## Execution checklist

- [ ] New native project and isolated development configuration, policy/target guards.
- [x] Typed trip/place/note identities with no competing raw itinerary storage on the connected native path (checkpoint 2).
- [x] Single enforced active-trip edit/checkpoint/save/conflict owner, correct geometry/presentation dependencies (checkpoint 2 local acceptance).
- [ ] Per-entity local persistence, durable dependency-aware outbox and small feature projections; no whole-account graph.
- [ ] Ordered account lifecycle and separated transport/repository responsibilities.
- [ ] Native backend authorization, revisions, receipts, bounded reads/deletions and current business rules.
- [ ] Native auth, StoreKit, catalog, support, deletion and diagnostics connected.
- [ ] Current local map uses indexed region reads and measured candidate handling without silent data loss.
- [ ] New-target deployment, integration checks, backup/restore/retention and operations configured.
- [ ] All Phase 1 exit conditions evidenced; remaining external gates stated explicitly.

## Verification record

### Detail-download and existing stop-note Save slice (September 14)

Owner asked whether these were reproducible problems before implementing the next cost slice. **Yes: measured the original source first, then implemented a narrow repair. This slice is complete for local/emulator acceptance at 00:42 on September 14; no cloud deployment or Phase 1 acceptance is implied.**

Reproduction and cost boundaries:

- Before the change, the real iOS SDK worker recorded `trip → saveTrip → trip` for one existing-note Save, and `trip` even with an empty queue. The baseline simulator result passed its explicit old-behavior assertions: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-hhynrkbvwykbnjbjcretlylmwnuw/Logs/Test/Test-BarkRanger-2026.09.14_00-19-31--0400.xcresult`. This is an observed redundant-download path, not an inference from an unrelated passing suite.
- The Firestore emulator reproduction uses the actual executor and trip/read handlers, instruments every transaction document read/write, and seeds 500 distinct places with existing notes. Changing one note through the old full-save command measured **1,006 reads / 5 writes**, including itinerary replacement. The new command measures **6 reads / 4 writes**, with no place or itinerary reads/writes. Two changed notes measure 7/5; 500 changed notes measure 505/503. Receipt replay measures 1 read / 0 writes. These counts include command authorization, rate admission and the operation receipt.
- **Do not report six reads as the whole save workflow.** One full detail confirmation still costs 504 reads / 1 admission write in that fixture (500 notes + itinerary/metadata + read admission). Combining actual handler measurements for the observed SDK sequence gives **2,014 reads / 7 writes before, 510 reads / 5 writes after**: about 75% fewer reads, not constant total cost. Counts exclude unrelated account refreshes, index billing, transport overhead and retries; they are local logical operation counts, not an invoice, cloud latency or 100K-user throughput certification.

Source-level implementation:

- `NativeTripSync` submits the durable intent directly; exact trip/note preimages are checked atomically by the server, so a preflight read cannot improve its correctness. Empty, conflict-blocked or deferred queues do not download detail. One canonical post-command read remains so unchanged-by-us notes edited on another device are correctly reconciled. Uncertain outcomes retain the same sealed operation identity/bytes.
- `NativeTripFeature` owns metadata and durable delivery, not selected-detail loading. Removed its independent selection/foreground detail reads. `ActiveTripSession → NativeTripRepository` remains the detail owner and reuses the exact metadata-version cache stamp. A reconnect retries an unavailable active selection through that cache-first owner, even if the older trip is outside the library head. Metadata changes invalidate detail without requiring an itinerary revision change.
- New small `NativeTripNotes` payload and `saveTripNotes` handler send **one atomic batch** of changed, already-linked stop notes. The backend verifies the itinerary revision, every edited note's revision and ownership/link, and aggregate UTF-8 logical size before writing anything. It advances metadata and edited-note revisions, leaving itinerary revision unchanged. This is not a loop of independent `editNote` calls, so a stale second note cannot cause a partially saved first note.
- The existing outbox, acknowledgment, projected successor preimages, conflict recovery and dependent deletion now distinguish a note Save from an itinerary Save. Existing durable `.save` intents keep their original semantics and sealed bytes; no store reset or new base schema. Untouched remote revisions are never borrowed by queued authored work. Save/Cancel, routing dependencies, current UI and product limits remain unchanged.
- Classification restores the authored base's stop-note text and compares the existing whole-trip fingerprint. It does not guess that unchanged routing means unchanged itinerary. **First notes that add references, day notes (currently embedded in the itinerary), mixed/structural edits, uncertain legacy bases and explicit conflict recovery still use one full atomic save.** Optimizing those paths would be additional work; this slice does not claim to have made every note-like field a constant-cost save.

Final verification: **103 simulator test definitions / 108 runs passed, zero failures or skips**, including five actual UI workflows, real native SDK calls, disk-failure/restart, note→note→itinerary→delete dependencies, untouched remote-note adoption, conflict/deletion handling, account isolation, schema upgrade and reconnect without a metadata event. Result: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-ffoguefragodmsfjofaxlpmrhsaj/Logs/Test/Test-BarkRanger-2026.09.14_00-36-54--0400.xcresult`, completed 00:42:24. The corrected focused run also passed but is not added to those counts. The complete backend run passed **36 checks** with serial suite execution, the domain package passed **58 tests**, and the disjoint deployment guards passed **4 checks**. Final Debug build-for-testing and generic simulator Release build passed; touched-source strict formatting and whitespace checks passed. Reviewed the final native Planner save/relaunch screenshots in `/tmp/BarkNoteSaveScreens.3KySwr`; no UI redesign. Backend reproduction remains executable in `01-code/functions-native/tests/integration/trips.test.cjs` with Node 22 and the strictly local native emulators.

Verification corrections/limits (not hidden as successes): the first broad simulator run failed five definitions. Two new queue fixtures supplied changing creation timestamps and were corrected to preserve the original immutable timestamp; one older account-switch assertion still waited for `PersonalState` in a native-only fixture and now verifies the native UID plus absence of that retired graph. Two UI checks required the missing loopback `profile-ui@native.invalid` fixture; restored it with the existing strictly local seeding script, not a live Premium grant. The final complete simulator run passes. An overlapping backend/simulator run also returned `Transaction is invalid or closed` in an unchanged activity handler; the isolated serial complete backend run passed. This is not concurrent/load acceptance. An accidentally selected legacy-emulator script refused the wrong/missing environment as designed; it was not pointed at the native emulator or counted as native acceptance.

Still deferred: canonical confirmation's full-note read, first-note/day-note save costs, archive-wide trip bootstrap, closed activity-history reconciliation and the remaining native/Apple/operations work. No speculative journal/dog/media implementation, new persistence layer or generic coalescing framework was added. All writes for this slice are local source or disposable `demo-bark-native` fixtures; existing production, web/users and Firebase cloud configuration are untouched.

### Correctness/coupling checkpoint — before download and note-save optimization (September 14)

Owner requested a checkpoint after stale/deleted-trip handling, precise cache-version tracking and targeted library updates, before the separate expensive-work tranche. **This checkpoint is complete for local/emulator acceptance on September 14.** Stop before the next tranche. This is a local iOS correctness slice inside recovery checkpoint 4, not completion of that checkpoint, deployment or owner acceptance.

Source changes and ownership:

- Metadata-only tombstones now clear a reconstructible clean draft and its selected identity in the same store/cursor transaction. Dirty drafts, live unsaved buffers and uncertain queued saves retain their original preimages. A late point reply cannot revive a newer deletion or verified absence; an overtaken nonempty detail reply is reported as stale, never returned to the shared editor as an empty selection.
- `NativeTripCacheStamp` records the exact full-read **metadata revision** and lossless read time separately from itinerary revision and authored draft bases. It covers remote note-only updates. The stamp is a small optional field on the existing trip-cache row; library-page freshness checks inspect the stamp, not itinerary or note bodies. Native local schema 1.1 uses an additive upgrade in the existing account directory. Old unstamped clean details require revalidation; existing drafts and sealed operations are not reset or migrated into new identities. Cache eviction is not deletion. A newer acknowledgment with no matching detail retains its accepted preimage; a confirmed deletion is handled separately.
- An older archive rebuild preserves newer point reads and their clean selected drafts. Confirmed deletion identities also travel with bounded invalidations, so removing 100 old trips cannot leave ghosts in an already-open library merely because the 24-detail/250-metadata caches evicted their records. Previously displayed deletion revisions fence late page replies. Overflow requests a bounded library reset and cancels an obsolete page request; it does not create a growing event queue or reset unrelated activity history.
- `NativeTripFeature` consumes changed identities instead of re-querying every loaded row. Ordinary note checkpoints do not invalidate the library; title/day/stop changes refresh the bounded local draft projection. Unchanged library values are not republished, and an unrelated trip's metadata does not bump the selected-editor revision. Archive changes update already-presented rows, rather than implicitly populating the UI with the whole archive. Explicit library pages remain independently paged. The unused whole-presented-ID metadata helper was removed.
- Existing `NativeStore`, account-scope owner, `ActiveTripSession`, editor, sealed command protocol and current UI remain the authorities. No new persistence actor, generic sync framework, journal/dog/photo feature, backend command or provider activation was added.

Final verification:

- **63 test definitions / 65 parameterized runs passed, zero failures or skips** on the final source. This includes actual native SDK integration, shared Map/Planner deletion, an overtaken SDK detail reply, both on-screen trip workflows, disk rollback, account isolation, lost replies/queued preimages, a real schema-1.0 on-disk store upgraded to 1.1, a 500-row library, a 100-deletion batch, and bounded coalescing. Result: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-gfkquthwzctedacuxqrugikglcon/Logs/Test/Test-BarkRanger-2026.09.14_00-05-16--0400.xcresult`, completed 00:07:39. Earlier focused and connected runs also passed; these final counts do not add duplicate runs from them.
- **Three cross-feature activity checks passed**, zero failures/skips: missed-change reconciliation using the actual SDK, the 1,000-walk bounded cache/write workload, and activity deletion/coalescing/overflow. Result: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-hlafyjktoibwxbfppgychdkshmnl/Logs/Test/Test-BarkRanger-2026.09.14_00-07-58--0400.xcresult`, completed 00:08:09.
- Final Debug build-for-testing, generic simulator Release build, strict formatting checks on the touched application sources, and whitespace checks passed. Reviewed editor and switcher screenshots from the earlier successful UI run in `/tmp/BarkTripCorrectnessScreens.maK0cy`; no UI redesign was performed.
- Restarted the disconnected local emulator suite under Node 22 and verified its native exact-target guard; only disposable `demo-bark-native` fixture state was reset. No cloud API/configuration/deployment or paid load run was involved. Tests are supporting evidence for the source-level ownership/version/invalidation fixes, not proof of whole-app or 100K-user readiness.

The large-library measurement observed **zero library-metadata lookups for a local note edit or title edit**, one changed metadata identity for an unrelated remote rename, no unchanged-library publication for the note, and no selected-editor invalidation for that unrelated rename. These are local projection/invalidation measurements, not zero total local I/O, Firestore billing measurements, launch-throughput evidence or 100K-user certification.

Still deliberately deferred: redundant remote detail downloads, unconditional foreground selected-detail reads, archive-wide bootstrap workload, closed-history demand, coherent multi-note/note-only saves and their server read/write measurements. The existing whole-trip save/acknowledgment protocol remains in use. Next tranche must not simply plug `editNote` into a queue whose accepted trip revision currently must advance by one. Preserve atomic multi-note Save/Cancel, retry identity and independent note preimages. Apple/native-service, deployment and physical-device gates remain as previously recorded. Existing production, users, web app and Firebase cloud configuration were untouched; verification uses only temporary local data and `demo-bark-native` emulators, with no paid load run.

### Post-checkpoint 3 source review — summary-first browsing (September 13)

Completed the [current native source/cost architecture assessment](../reports/ios-native/SUMMARY_FIRST_SOURCE_REVIEW_2026_09_13.md) in response to the owner's cheap-overview/on-demand-detail request. **Review and plan changes only: no implementation, test execution, deployment, cloud load spending or existing-system changes in this pass.** Checkpoints 4–5 remain incomplete. The recovery plan now puts the targeted cost gate inside checkpoint 4, not in another rewrite or additional future-feature phase.

Source findings: official pin selection already uses the local catalog; device bookmark selection uses local files, not Firestore. However, foreground/reconnect can fetch selected trip content and all referenced notes; cold/expired trip metadata synchronization can page through the full archive despite a bounded local cache; a persisted activity-history cursor enables closed-screen change refresh; read admission itself reads two documents and writes one rate row; a note-only explicit Save still validates/replaces the selected itinerary and reads its referenced place/note set. These are concrete remaining work items, not repaired by passing earlier checks.

Clarification of checkpoint 3's resource language: a never-opened walk archive is not downloaded at startup, but after history has been opened its persisted cursor can enable subsequent background reconciliation. Explicit history pagination is screen-owned; an expired activity cursor can also trigger a first-page recovery read during feature refresh. “Four bounded pages per pass” is not a bound on total continued synchronization. Local touched-row/payload measurements below are not complete server billing measurements.

Wider review confirmed useful account/edit/persistence ownership and server authority. Remaining native implementation gaps include actual StoreKit services, support submission, complete server/local account deletion, App Check/provider activation, independent catalog delivery, schema upgrade/recovery and operational/deployment acceptance. The configured catalog still targets the old public bucket; no old resource was changed. Future journal/dogs/photos/permanent tracks remain unimplemented roadmap items. Do not call the rebuild complete, all browsing free or 100K-user costs verified.

### Recovery checkpoint 3 — connected visits/walks/progress (September 13)

Status: **checkpoint 3 complete for local/emulator acceptance at 21:07**. Debug and Release simulator builds pass. Stop before checkpoint 4. This supersedes earlier “unconverted visits/walks” and unresolved activity-cursor labels below; it is not native deployment, physical-device acceptance, 100K-user certification or whole Phase 1 completion.

Sequence: repair the activity missed-interval prerequisite; connect native visit markers, selected history/actions and progress; connect activity/run commands, explicit conflicts, recorder/Health handoff and paged history; connect the existing five-row leaderboard/personal standing; remove the superseded account-wide native consumers; then run source, adverse-state, SDK/UI and bounded-resource verification. Reuse the accepted account lifecycle and single transaction writer. No deployment, existing-system changes, future journal/dog features or cloud load spending in this checkpoint. Measure per-action work against growing history; do not equate a five-row leaderboard with constant-cost personal rank.

Connected source and removed native runtime paths:

- `AccountAssembly → AccountSession → NativeVisitFeature / NativeExpeditionFeature` uses the existing UID-scoped `NativeStore`, not another database writer or identity owner. The account clears old visible features before draining their jobs/transports and closing the writer. Native accounts and guests return before opening `LocalStore`; its old account graph construction is DEBUG-only. No native visit/walk screen uses `PersonalSnapshot`, `UserValue`, old `VisitRepository` or old `ExpeditionRepository`. Historical fixture/recovery source remains; complete legacy/schema cleanup is checkpoint 4, not a claim that every old file was deleted.
- Map visit actions, Passport totals/badges, history/date editing, reviewed bulk removal and daily presence use typed native commands. Dates preserve original evidence tier; selected removals bind the reviewed event/slot revisions. Pending marker presentation is separate from server-confirmed points. Conflicts show the saved values and require an explicit choice, including permission before recreating a remotely removed visit manually.
- Walk assignment, manual/GPS/pedometer summaries, explicit Health imports, corrections/removal, completion and latest-completed-trail display use native activity/run records. History preimages belong to durable intents, not an evictable cache. Stable activity identities, original measurement intervals and run attribution never change during retry/recovery. Settled historical conflicts block their connected dependency group, not unrelated walks or today's different trail. An uncertain sealed command is resolved before another write is sent; it is never silently discarded.
- Recorder cleanup follows successful durable native handoff. Full-disk handoff failure retains the original recording; cleanup failure and repeated recovery retain one queued summary. Confirmation of a completed trail waits for pending run changes to become confirmed. Pending mileage is shown separately rather than added to a newer confirmed total after a lost reply. Existing zero mileage points / one completion point and frozen completed-run attribution are preserved.
- History pages are requested only by the history screen. Leaving it releases extra display pages; the reconstructible cache stays bounded. CSV export streams explicit fifty-row cloud pages into a protected temporary file, overlays the captured pending choices, and rejects a moving archive rather than handing off a partial result. Passport sharing uses typed progress, not an account-history blob.
- The native leaderboard returns five intentionally public entries plus only the authenticated caller's standing. Hashed public identifiers avoid exposing Auth UIDs. Existing positional top-five/shared outside-rank tie behavior is retained. A failed personal lookup preserves the five fetched leaders. One private replaceable rank-cache row reuses the higher-score count for sixty seconds; no global fan-out ranks or continuous full-board listener were added.

Defects found by tracing source, then checked at their failure boundary:

- A newer point read of activity A could previously cause a delayed scan to skip B's update/deletion. An overtaken scan now persists a restart from its **unchanged completed lower bound**, not the newer point-read time. Accepting an acknowledgment also refuses to dequeue if its activity detail would fail the stale-read fence.
- A canceled visit action could still enter the writer and commit. The sole writer now checks cancellation at entry and immediately before committing; failed work rolls back.
- An unknown offline expedition selection was indistinguishable from confirmed “no current trail.” The local cache explicitly remembers confirmed absence; a never-downloaded selection cannot authorize recording against an invented empty context.
- Activity invalidations coalesced while the history screen was busy could lose deletion identities. Bounded merged invalidations now carry a reset signal on overflow. History page generations prevent an old in-flight page from restoring cleared results.
- A remote visit-date change could invalidate its cached detail without replacing the row in an already-open history screen. Newly advanced marker revisions now invalidate that screen's pagination and request a fresh bounded page. Unchanged markers and locally acknowledged revisions do not cause that extra reload. Visit presentation uses one bounded cache scan and compact marker pages rather than a database lookup for every displayed archive row.
- Completion-list observations could recursively request themselves. Only a newly accepted completion requests that list; failed reads retain the refresh request. Conflict resolution verifies the exact review and recomputed typed plan, rejects new dependencies outside the review, and atomically replaces only the reviewed queue positions.

Resource evidence / limits:

- A synthetic **1,000-walk** local history loaded through twenty fifty-row pages retained **250** cached activities. Staging one further manual walk changed **one pending-operation row plus one sequence-metadata row**, encoded **less than 1 KiB**, and emitted **zero profile invalidations**. These are measured logical SQLite/SwiftData changes, not physical disk blocks or Firestore billing measurements. Pending authored data is outside the evictable cache.
- Normal startup reads current selection/progress/official-site markers/latest completions, not the activity archive. Incremental scans use at most four bounded pages per pass and resume with durable cursors. The explicit Health picker checks at most 100 candidate identities; overlap admission queries one neighboring interval. Per-account serialization avoids cross-user write contention; none of this proves launch throughput.
- The synthetic five-row plus personal-standing backend reply measured **860 bytes**. Outside-top-five reads use at most two additional point documents plus the rank index aggregation and fixed admission work. A cold rank count is **not constant-cost**: Firestore bills aggregation work in batches of up to 1,000 index entries, with a minimum charge. At 100K published entries, an uncached count can approach 100 billed read units for the aggregation alone; that is a pricing-model inference, not a cloud-load measurement. See [Firestore aggregation pricing](https://firebase.google.com/docs/firestore/pricing#aggregation_queries) and [aggregation query behavior](https://firebase.google.com/docs/firestore/query-data/aggregation-queries). Monthly cost still depends on active usage, cache reuse, indexes and deployment settings.

Verification:

- Final complete checkpoint run: **50 test definitions / 54 parameterized runs passed, zero failures or skips**, including both actual-screen workflows and the new remote-date refresh/full-disk handoff checks. Evidence: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-gwdouuhudirwildbzvviltzhhmsz/Logs/Test/Test-BarkRanger-2026.09.13_21-03-18--0400.xcresult`, completed 21:05:24. The earlier full 20:52 run passed 49 definitions / 53 runs before adding the source-discovered remote-date case. Test counts are supporting evidence, not the reason the ownership/query design is accepted.
- **36 account/trip/isolation/job-lifecycle regression checks passed**, zero failures/skips. Evidence: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-daasgnlwgslfhbgbceexahllkllo/Logs/Test/Test-BarkRanger-2026.09.13_21-01-46--0400.xcresult`. Four checks in the earlier 20:58 run still waited for the deliberately removed account graph (or its transitional-directory error). They now await the native scope, assert the old graph stays absent and retain their original late-write, checkpoint and account-isolation assertions. The obstructing old-file fixture remains byte-for-byte intact.
- Node **22**: **35 backend/target-isolation checks passed** (24 serial Firestore integration, 8 unit, 1 actual-callable/rules, 2 deployment-target checks), zero failures/skips. Native rules deny direct visit/activity/claim/run/rank-cache listing and writes; authenticated bounded reads own those contracts. **57 domain tests passed**. Debug build-for-testing, the generic iOS Simulator **Release build**, and whitespace checks passed. This does not assert device signing, App Store eligibility or production provider readiness.
- Actual iPhone 11 simulator screens exercised map marking, Passport visit selection/date dialog/removal, top five plus own standing, trail assignment, manual entry, correction, relaunch persistence and removal. Reviewed exported screenshots of the leaderboard and corrected history; layout/scoring limits were not redesigned. Evidence screenshots: `/tmp/BarkNativeAdventureScreens.pe3UV9`, originating in the 20:45 UI result bundle.

Native cloud deployment, 100K-entry/load measurements, schema freeze, permanent retention/deletion operations and real outdoor/Health hardware verification remain later gates. No production project, web app, users, owner-device installation, Apple activation or paid load run was changed/performed.

### Recovery checkpoint 2 — connected trips/notes/map (September 13)

Status: **checkpoint 2 complete for local/emulator acceptance at 19:31**. Stopped before checkpoint 3. This is not native cloud deployment, physical-device acceptance or Phase 1 completion. The historical trip-foundation entries below are superseded by this section.

Connected and removed paths:

- The actual Map and Planner use `AppComposition.activeTrip → ActiveTripSession → TripDraftSession → NativeTripRepository → NativeStore`. One shared editable buffer owns selection, checkpoint, save, duplicate, clear, delete and conflict decisions. Screen models contain presentation state, not another saved-trip authority. `NativeTripFeature` owns metadata paging and its account-bound worker; it shares the profile scope's transaction writer, not a second store or identity lifecycle.
- Save captures typed trip/note preimages, commits an intent, seals its exact request bytes before sending, then accepts cache/preimage updates without overwriting subsequent edits. Source review fixed a real UI bug: a base-only server acknowledgment previously restarted the editor and erased its save confirmation. It now advances the comparison base without resetting current presentation or typing. Note/time edits reuse existing route geometry.
- Library requests load ten metadata rows at a time and selected details; normal list presentation never decodes every itinerary/note. Selected-trip absence and completed replacement scans remove verified stale presentation rows; cache eviction alone is not treated as deletion. Paging is explicit. Reconnect/background/identity changes cancel and drain the relevant work before reopening admission.
- Removed raw `TripDraft.expected`, the old whole-library presentation builder and the map's legacy-trip fallback. The old `TripRepository` and account trip-paging hooks are now DEBUG-only historical regression code, excluded from Release and never constructed by the native factory. `LegacyTripDraft`/`TripRecordCodec` remain only for one-time earlier-device recovery and historical, unconverted adapters; they are not a fallback native data model. Removing recovery decoding would strand existing guest files. A durable import receipt prevents decoding those files again in steady state.
- Guest planning now opens a typed native store, without opening the account-wide `LocalStore`. Guest handoff claims work for one destination before copying, uses destination receipts and never creates cloud-save intents. Different content with a colliding ID fails without silently merging either version. Original saved-place file IDs, stop IDs and notes are preserved; nothing is uploaded or rekeyed by this checkpoint.
- Conflict/delete choices bind the displayed draft, revision and exact pending-operation IDs. A newer edit makes an older choice invalid. Recovery of remotely deleted work explicitly creates a new trip ID. Selected trip/day remain device-local. Failed local writes cannot report success or discard the user's buffer.
- Ordinary authentication changes wait for checkpoint and refuse to change identity on failure. Forced authentication changes immediately hide private data and retain failed-checkpoint work in memory for its original scope, never the next UID. Returning to that scope restores it. The error explicitly states this last-resort memory buffer cannot survive closing the app when storage is unavailable; it is not a claim of durable full-disk recovery.
- `SavedPlaceStore` is the sole device-bookmark writer, retaining original JSON plus a derived SQLite R-tree index. `SavedPlacesModel` holds viewport marker metadata and one selected full bookmark. Mark-before-write recovery rebuilds a dirty index after interruption. Exact region filtering handles date-line boundaries, and point lookups retain active/selected identities without duplicating official stops. Notes are excluded from marker payloads; arbitrary map pin caps were not added. Malformed authored records and ambiguous identities fail closed without overwriting source files. Arbitrary corruption of the SQLite file itself is also fail-closed, not an implemented automatic cache-quarantine path.

Measured work (isolated iPhone 11 simulator; not production cloud or physical disk-write measurements):

- A 500-stop trip's one-note edit changed **one `Draft` row**, encoded **216,341 bytes** for its local draft and **144,065 bytes** for its explicit account-save command. The save created a pending row plus sequence metadata; sealing updated the pending row. There were **zero unrelated profile invalidations**. These opt-in DEBUG probes count logical changed rows, not filesystem block writes.
- This command is **not a field-only note update**. Current Save intentionally sends the selected itinerary's references plus changed note bodies to retain whole-trip atomicity; the other 499 unchanged note bodies were absent. Cost still scales with the selected itinerary, not with all account history. An earlier assertion checked a nonexistent `content` key and was replaced with checks against the real `days`/`notes` wire fields. Do not borrow the backend's separate direct-note-handler read count as a measurement of the current UI save.
- The real SDK, two-stop connected edit/save trace encoded **1,000 bytes**, included one changed note body and made **zero additional directions requests**. Bookmark indexing of **10,000** fixture records returned **9** visible marker rows (**2,191 bytes**) in approximately **0.16–0.19 ms** across the final two warm regional query runs. A world query returned all 10,000, confirming no hidden result cap. This does not establish that drawing 10,000 MapKit annotations is acceptable or that 100K-user capacity is proven.

Verification and limitations:

- Final complete run: **89 test definitions / 92 parameterized runs passed, zero failures or skips**, including 84 app definitions and all five UI workflows. Evidence: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-ahfguqtwsmehxpdbwrwoutpnwblg/Logs/Test/Test-BarkRanger-2026.09.13_19-25-32--0400.xcresult`, completed 19:30:52. The 19:16 run's only remaining failure was the historical account-screen test assuming the reusable emulator fixture still had Standard appearance after a preceding run saved Satellite. That driver now changes the actual current setting, verifies it after relaunch and uses a unique display name. No production behavior was changed for that fixture issue.
- App checks include real Firebase SDK save/read/receipt/conflict/recovery, offline/reopen, uncertain replies, stale typing/selection/delete/conflict choices, queue rollback, account isolation, failed checkpoint/auth transitions, guest import/collision, archive rebuild/cache pressure, date-line/index boundaries, interruption and original-file retention. Existing editing, duplicate, navigation, access and route-reuse checks use the connected owner. This is a focused checkpoint suite, not a claim that all unconverted feature fixtures pass.
- Debug app/test and Release simulator builds passed. Domain checks: **57 passed** (existing redundant-require fixture warnings remain). Backend unit checks: **8 passed**; real Auth/callable/rules transport: **1 passed**; native/existing deployment isolation: **4 passed**. The first parallel-file backend integration run passed 22/23 with one invalid/closed emulator transaction; the failing visit file passed alone, then the full serial-file run passed **23/23**. The initial emulator failure's root cause is unconfirmed; transaction-race scenarios remain concurrent within their test files. No backend implementation changed in this checkpoint.
- Reviewed actual Planner save/relaunch and device-bookmark saved/star/relaunch screenshots. Evidence is retained under `output/native-rebuild/checkpoint-2/`, including the initial real save-confirmation failure and later successful screens. Format/lint and diff checks passed for the touched native implementation.
- Visits/walks/progress/leaderboard remain checkpoint 3. The known activity change-cursor missed-interval bug remains its prerequisite. Signed-in unconverted feature storage/publication still exists and has no legacy cloud transport in the native factory. Native schema freeze/upgrade verification, catalog/services/Apple implementation, deployment/provider setup and real-device/dense-map/production cost acceptance remain checkpoints 4–5. Future journal, multiple dogs, media and permanent trail archives were not implemented.
- Existing production projects, backend, users, web app and secrets were not modified. No live deployment, cloud load spending or owner-device installation occurred. Local emulator fixtures alone were mutated.

### Recovery checkpoint 1 — connected account/profile (September 13, accepted before checkpoint 2)

Status: **checkpoint 1 complete for local/emulator acceptance at 17:44**. Stopped before trips. This is not native cloud deployment or owner/device acceptance, and Phase 1 overall remains in progress. The historical entries below describe earlier isolated foundations and are superseded for account/profile by this section.

- Connected actual `AccountAssembly → AccountSession → AccountModel/AccountView/SettingsRepository` to the typed native profile store/worker/commands. Name, standard/satellite appearance and native access no longer travel through a whole-account dictionary. Native data is not mirrored into the old `PersonalState`; the current account screen has no old profile fallback.
- One account lifecycle owns immediate visible-scope clearing, ordered cancellation/drain/close and next-scope opening. Profile sync now uses reusable pause/resume and terminal close. Source review corrected cancelled-flight retirement: a completed old drain retires only its own operation IDs, never a later generation's task.
- Removed account-wide membership/conflict presentation, the generic profile/activity conflict router and old subscription-management URL actions from the account UI. Replaced the obsolete `AccountUITests` screen driver with the new native account flow. The old `AccountSyncConflict.swift` file was removed; domain-specific conflicts remain owned by their features. No existing cloud data or production services were deleted.
- Native profile opening is independent of unconverted local files. Temporary signed-in `LocalStore` data is isolated under `native-feature-transition/<project>`, while guest planning remains separately retained. A deliberately obstructed transition directory cannot prevent native bootstrap, access refresh or a name save. Unconverted feature intents no longer request unrelated native profile refreshes.
- Profile conflict decisions bind both the displayed confirmed revision and the exact reviewed pending-operation IDs. Later typing invalidates an older choice. Keeping local creates new operation identities; uncertain commands retain their sealed bytes. No automatic merge or account-wide overwrite.
- Repeated identical name/appearance saves are no-ops at the writer boundary. The 128-operation cap fails explicitly without discarding work. Access expires without a fresh server read and cannot be borrowed from another UID or a sandbox entitlement. Known transient failures retry; malformed/unsupported/permission failures require attention instead of becoming an automatic read loop.
- Final verification: **28 test definitions / 31 parameterized runs passed, zero failures/skips**, including the actual native screen create/sign-in/name-save/appearance/relaunch/sign-out workflow and real SDK account actions, cache obstruction, offline/reopen/lost reply/conflict/stale-choice behavior, failed local writes, validation, queue bounds, expiry, stale scope, guest isolation and unchanged settings/planner guards. Evidence: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-bssqbpabivyvpvfmdykmeejimzfp/Logs/Test/Test-BarkRanger-2026.09.13_17-42-12--0400.xcresult`. Reviewed the saved-profile and signed-out appearance screenshots; copies are in `output/native-rebuild/checkpoint-1/`. Debug app/test-target and Release simulator builds passed. Touched native Swift formatting/lint and diff checks passed. The real native callable/rules transport check passed, as did eight backend unit checks, 23 backend integration checks, four deployment-isolation checks and 57 domain checks. Existing redundant-require warnings remain in domain fixtures.
- Initial UI-driver failures were diagnosed from rendered recordings and accessibility/event evidence: iOS 26's remote password service ignored a tap addressed to the host process, and the Settings picker exposes a combined label. The test targets the actual credential-service process without saving test passwords and uses the actual picker label; the app's password behavior and UI navigation were not changed to make a test pass. `seed-native-profile-ui.cjs` refuses non-loopback/non-demo targets. `test-ios-accounts.py --native-profile` creates only a temporary test manifest and runs this focused native workflow.

Temporary code at checkpoint 1 (trip status superseded by checkpoint 2 above): `LocalStore`/`PersonalState`, `TripRepository`, `VisitRepository`, `ExpeditionRepository`, their old observation/paging branches and the DEBUG historical emulator remained for unconverted features. `ProfileRepository` remains a historical emulator fixture dependency; it is not connected to native profile actions. The native factory passes no `CloudUserTransport`. Account deletion's old implementation is disabled and must be replaced in checkpoint 4. None of these adapters establishes working native visits, walks, leaderboard or support. The known activity-reconciliation skipped-interval defect remains checkpoint 3's prerequisite.

Engineering assessment: this completed data path has clearer authority and smaller, independent records. The whole app is still transitional, and `AccountSession` still has unconverted feature responsibilities. Do not call the entire architecture finished or claim 100K-user readiness from these checks. Profile requests have bounded exact-document reads and bounded pending work; no production workload/cost/load claim was measured here. No live deployment, existing-system change or cloud load spending occurred in this checkpoint.

- Baseline domain suite: 51 tests passed before implementation.
- Deployment isolation: 4 checks passed; native/existing configurations reject each other's projects.
- Typed Trip/Day/Stop and stable PlaceIdentity added; production map/planner property reads converted. The domain suite passed 51 checks after this change; 3 additional native model/identity/route-input checks passed.
- iOS simulator application build succeeded after the typed model and shared-owner changes. Test-target adaptation is still in progress; no claim of full regression success.
- Map/Planner constructors now require an explicit shared ActiveTripSession. Session draft internals and repository-operation closures are private; save/select/delete/day-edit commands moved into the shared owner. Further source review and race checks are required.
- Temporary TripRecordCodec keeps the not-yet-replaced account adapters compiling without retaining raw fields in the editable model. This bridge must leave the native runtime graph before Phase 1 completion. Local live account namespace changed to `BarkNative-v1/bark-ranger-ios`; the live project guard now rejects previous registration.
- Native backend contract validation/fingerprints/place identity: 3 checks passed. Feature handlers and the connected native persistence/sync graph are not implemented yet.
- Saved-place identity now follows stable private/provider identity. Existing device bookmark-file rekeying must be handled safely during the indexed-store replacement; do not claim that compatibility path is complete.
- Focused shared-owner/planner simulator checks passed (17 tests / 19 parameterized runs, zero failures); this is not a full regression result. Save completion now checks account scope and edit revision without taking ownership of a newly opened trip.
- Swift/JavaScript place-identity encoding now shares an explicit flat wire shape and Unicode/hash vectors; focused domain wire checks passed. Native backend unit checks passed 6 tests; real Firestore emulator profile transaction checks passed 3 tests covering receipt replay, concurrent revisions and bounded reads. No cloud functions deployed.
- Native per-entity profile/entitlement rows and a dependency-aware durable outbox are under construction, not connected to the app. Initial simulator checks exposed a Core Data reserved-key collision (`entity`); renamed the row field to `entityKey`. The four original persistence checks then passed. A fifth explicit-conflict-choice check passed after adding atomic recovery with new operation identities, stale-choice rejection and rollback. Do not treat these checks as full feature integration.
- Added the native `nativeCommand` callable with exact-project/Auth/App Check admission, registered iOS app restriction, bounded request size and privacy-safe versioned errors. Eight backend unit checks and an end-to-end Auth/Functions/Firestore emulator check passed, including owner-only exact reads, rejected cross-account reads/listing/direct entitlement writes, exact replay and free-account write rejection. Native rules now permit only owner profile/entitlement `get`; remaining client paths stay denied. These rules/functions have **not** been deployed to the new cloud project.
- Added an account-scoped iOS profile transport, coalescing profile sync worker and isolated native emulator factory. Application/test targets compile. Actual iOS Firebase SDK checks passed against the isolated Auth/Functions/Firestore emulators: durable bootstrap, exact receipt replay, owner reads, no fabricated paid access, coalesced refresh and stale-account rejection. The latest combined run passed six checks (five local persistence plus one SDK integration), with no skips. The app still uses the temporary old runtime graph behind the native project guard, so this is not a connected native product yet.

## Historical foundation work before the connected feature checkpoints

### Trip slice progress (September 13, continued)

- Native backend now implements atomic trip/place/contextual-note creation and save, independent note updates, selection and deletion. Notes have one saved body; duplication retains current independent planning-note semantics. Removed stops/trips unlink notes without erasing their text. Public catalog validation uses a reviewed copy of the bundled public catalog, not another project's account database.
- Added bounded native reads: selected itinerary/current note context, ten-row metadata library pages, and 100-row reconciliation pages ordered by exact server timestamp/document ID. Scans retain a server-confirmed upper bound, overlap the previous completed boundary and report an 89-day supported horizon before the 90-day deletion TTL. New indexes/TTL exemptions are local configuration only, not deployed.
- Latest backend emulator run passed 10 checks, including concurrent page updates/deletes, equal-time ordering, owner admission, 500-stop creation and 500-note bulk update. Emulator evidence does not establish cloud limits or the 100K-user launch workload.
- Native local trip/content/note/draft rows, exact timestamp cursors, sealed dependency-aware trip saves/deletes, typed read/command adapters, and per-trip worker are implemented but not connected to the existing screens/lifecycle. Confirmed cache and dirty work are separate. Queue successors retain their own expected note revision rather than borrowing an unrelated later remote revision.
- Initial cache candidates: 24 clean trip details / 12 MiB including note payloads, 250 metadata records plus selected metadata, and 24 clean drafts plus selected draft. Existing twenty-dirty-draft protection and 128 pending-operation cap remain separate. These are cache budgets, not remote archive/UI limits; device workload measurement is still required.
- Reconciliation persists the page and next request atomically. A bounded unpublished rebuild generation is promoted only at completion; dirty drafts and outbox survive. A simulator check exposed pending-insert/fetch-offset retention behavior; offset-based pruning was replaced by bounded candidate queries before eviction.
- The combined native local checks passed 10 tests with zero failures/skips: five profile persistence, four trip persistence, and one interrupted 300-record rebuild/cache-budget check. Evidence: `/tmp/BarkStableAnchor/BarkRanger-fcvvilwnbqlrknasqxhccpgusdwx/Logs/Test/Test-BarkRanger-2026.09.13_13-17-32--0400.xcresult`. New actual-SDK trip integration is under verification, not yet claimed passing.
- Remaining trip work includes current-screen/repository integration, selection synchronization, conflict recovery (including remote deletion), cache/selection invalidation on rebuild, coherent lifecycle scheduling and device measurements. Passing the isolated slice does not mean those gaps are resolved.
- Actual iOS SDK trip integration passed against the isolated Auth/Functions/Firestore emulators: bootstrap, emulator-only paid fixture (never a live grant endpoint), durable trip/note save, exact receipt replay, selected trip read, metadata library page, reconciliation page, local cache rebuild/refill, and stale-account rejection. Combined run passed 12 checks with no skips/failures. Evidence: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-gepfdncpvpthkuaxyzaeolnmspow/Logs/Test/Test-BarkRanger-2026.09.13_13-21-03--0400.xcresult`. Subsequent source review tightened rebuild invalidations, cursor validation and newer-point-read/cache-generation overlap; those refinements receive another local run.
- Added explicit trip conflict recovery with stale-choice checks, durable replacement operation IDs and failure rollback. A remotely deleted trip is recovered as a new trip identity rather than silently reviving an old ID; the eventual caller must report that outcome. Added owner-scoped bounded note-preimage reads for recovery of detached planning context. Active-trip open/edit/clear/discard/delete transactions and small draft/pending list summaries are implemented in the native store; current UI integration still remains. Deletion uses the editor's captured base revision, not a newer unseen metadata revision. Repeating an unchanged save no longer queues another write.
- Formatting/lint passed for the new native store/domain files using the repository's Swift style. Local tests cover atomic deletion/selection rollback, conflict recovery, clean-save no-ops and unchanged pending-save reuse. A further source correction retains the full accepted trip note preimage during an unsaved stop removal, so Undo cannot lose its saved note revision; focused domain verification added.
- The deployment-runtime run also passed 14 actual-SDK/local checks with no skips/failures (`/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-cscssqytcxjcksaaehqzhfvqoccx/Logs/Test/Test-BarkRanger-2026.09.13_13-41-06--0400.xcresult`). Domain suite now passes 57 checks. Later review distinguishes unsaved Undo from clean cache even when it matches an older confirmed trip, and lets an acknowledgment advance only the stored base while a checkpoint retains the user's concurrent keystroke. Added a cache-pressure/acknowledgment check for those cases; current app-screen/lifecycle integration is still outstanding.

### Visit/progress slice progress (September 13, continued)

- Reviewed the existing visit, achievement and leaderboard source rules before implementing replacements. Native visits now have stable event IDs and a separate official-site slot, preserving one active event per site and current honor/proximity credit. Canonical public catalog facts are server-owned. Date correction does not change verification or rewrite the profile. Removal keeps earned badge evidence and clears current credit; deleted event records retain a 90-day tombstone while the official-site slot remains for stale-write protection.
- Added small incremental progress/state counts and fixed-definition award state, deterministic award documents and per-user public score projections. The current time-of-day/Christmas/marathon/state/rank rules are retained. Marathon evaluation queries only bounded neighboring dates (at most four records on each side), not the visit archive; previously earned badges require no historical award scan. Date-only changes do not rewrite the leaderboard. Daily streaks use server acceptance day and cannot be backfilled with a client creation timestamp. Public achievement definitions were copied into the isolated backend; no legacy backend imports were introduced.
- Single and atomic bulk visit-removal handlers are implemented locally. Bulk removal checks each selected visit revision before any mutation, with a 500-item bound matching the existing mutation selection contract. It does not expand repeat-visit behavior. The new bulk path is still undergoing verification.
- Typed native visit/progress/marker models and transport, fifty-row history reads, hundred-row exact-time marker reconciliation, per-entity local cache rows and durable marker cursor commits are implemented. Official-site marker tombstones are retained, so their sync path does not use the visit-detail TTL horizon. The 250-detail local cache is reconstructible and is not an archive/UI limit. Late history/point responses cannot reverse a newer marker update or deletion.
- Eight backend unit checks and fifteen integration checks passed before the latest bulk-removal refinement. The first combined SDK/local run passed seventeen checks and exposed a visit persistence failure. Direct field diagnostics found `deleted=false` on the local model while the encoded record correctly contained `deleted=true`; Apple's installed Core Data header reserves that property for object lifecycle. Renamed local visit/trip flags to `isTombstone`, retained the wire contract and added trip-library/reopen coverage. Reverification is in progress; the failing SDK run is not being reported as a pass.
- **Not connected yet:** native visit outbox/repository/conflict flows, existing passport/map screens and lifecycle scheduling. Preserve offline edits, atomic bulk selection and independent-entity conflict behavior while completing that integration. These backend/cache pieces alone do not complete the visit slice or Phase 1.
- The `isTombstone` repair passed the combined run: eight backend unit checks, sixteen backend integration checks (including bulk-removal atomicity), and eighteen real-SDK/local iOS checks with no failures/skips. Evidence: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-govbtvvlpsqkutbrtflwasrbsvht/Logs/Test/Test-BarkRanger-2026.09.13_14-39-30--0400.xcresult`. Native Swift formatting/lint also passed. Follow-on work adds explicit current-visit revisions to lightweight marker slots and one bounded selection read for bulk receipt/conflict recovery; it is not yet counted in that passing run.

### Visit outbox, current actions and recovery checkpoint (September 13, 15:22)

- The durable visit queue now owns recoverable before/after values independently of evictable confirmed history. Site overlaps define ordering; a rejected/conflicting park does not block unrelated parks. Atomic bulk removal remains one command. Queue inspection reads small metadata, and selected-set preparation decodes each relevant operation once, including a 500-site bulk value. Profile/trip/visit queues share ordering infrastructure without sharing a whole-account payload.
- Added typed current-visit selections and repository actions. Date correction/removal validate the event and revisions selected by the user; they do not borrow a newer remote preimage. A normal acknowledgment of the same event/revisions does not invalidate a still-open selection. A new mark after removal uses a new event identity. Upgrade retains the original visit date. None of these changes introduce repeat-visit UI.
- Added explicit connected-group conflict review and atomic resolution. The review contains original local, remote, and exact proposed values, and commit uses the same proposed operation identities. Keeping remote discards only the reviewed linked group. Keeping local preserves operation order and bulk atomicity with new command IDs. A remotely deleted event is not silently resurrected; date/upgrade recovery requires explicit permission to create a new manual event and reports that result. Old GPS is never relabeled as fresh; expired observation-based recovery requires a new observation. A sealed command with an uncertain outcome cannot be discarded/rebased. Current screens still need to expose these review/permission/error states.
- Source review found that independent history and marker feeds can legitimately overlap at different revisions. A newer history detail now causes a consistent selected-point refresh instead of a false corruption error or using an unseen slot revision. The real SDK scenario verifies this ordering.
- Centralized Firebase callable error normalization; SDK timeout/unavailable/cancellation errors now enter bounded retry handling. Profile calls use that shared transport too. No public Premium bypass or production fixture was added.
- The previous 14:59 checkpoint passed 21 real-SDK/local iOS checks. The latest combined run passed eight backend unit checks, sixteen backend integration checks, and **23 real-SDK/local iOS checks**, with no failures/skips. It covers durable lost-reply replay, offline mark/date/bulk chains, rollback during acknowledgment and conflict resolution, explicit remote-deletion recovery, stale-choice rejection, unrelated queue preservation, and cross-feed refresh. Evidence: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-gdxfopviqnakdnagrsvmdxtxlsaq/Logs/Test/Test-BarkRanger-2026.09.13_15-22-16--0400.xcresult`. Native visit formatting/lint passed. These are isolated-slice checks, not current-screen integration, production-scale measurement, or Phase 1 acceptance.

### Activity / virtual-expedition backend checkpoint (September 13, continued)

- Reviewed the existing production `expeditionMutations`, private ledger, domain policy, recorder/import boundary and history UI. Current behavior is zero points for newly logged miles and one point per completed virtual run. Manual-entry and measurement limits remain; editing an old run or a display name does not move credit to a new run. Latest completion per trail remains today's display, with each actual run retaining an independent identity.
- Added isolated native activity/run handlers, individual event and run records, a small lifetime/active-selection state, and independent permanent activity identity/interval evidence. No growing history, duplicate-ID or interval array is written. Duplicate imports remain blocked after event removal and receipt/tombstone expiration. GPS/Health paths remain on-device; these new records contain summaries, not raw samples or location paths.
- A nonmanual overlap check reads at most one indexed neighboring interval (end ordering handles a zero-length boundary tie). All accepted activity insertions also update one per-account state revision, forcing concurrent competing inserts to recheck overlap. There is no cross-account global lock. Private minimal evidence remains denied to direct client reads and must be included in account deletion.
- Added owner-scoped selected expedition/activity/run reads, fifty-item history pages, candidate-ID import checks capped at the current 100-workout picker, and bounded latest-completion-per-trail reads. New indexes/TTL settings are local and **not deployed**. Eleven virtual-trail definitions compare exactly to the existing iOS public resource.
- The combined backend run passed eight unit checks and twenty-three emulator integration checks, including seven new activity/run scenarios. No iOS activity transport/store/queue integration exists yet; this does not complete the expedition slice. After that run, tightened the history paging fixture to use valid matching event IDs and made the completion display explicitly use the run's server-owned catalog snapshot; these refinements need reverification with the forthcoming Swift adapters.
- Domain compilation/regression after the visit work passed 57 tests; pre-existing redundant-`require` warnings remain in domain fixtures. No cloud deployment or load spending occurred during these local checks.
- Added typed Swift activity/source summaries, virtual runs, small expedition state, selected snapshots, bounded histories/import-identity/completion lists, and explicit command encoding over the shared real SDK transport. Activity creation now stores authoritative `recordedAt`, separately from source start/end/display dates. Record-command run revisions always identify the captured assigned run, not a later unrelated active run. Swift decoding preserves the existing hundredth-mile rounding, including the maximum 500,000-meter recorded summary.
- The 15:45 combined run passed eight backend unit checks, twenty-three backend integration checks and **24 real-SDK/local iOS checks**, with no failures/skips. Actual Swift/Firebase calls exercised assignment, recording, replay, completion, correction, deletion, duplicate reimport, import-ID lookup, history, completed-trail display, maximum measurement decoding/round trip and account-change rejection. Evidence: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-cysgaxskyivtaafthhenvkzolbpn/Logs/Test/Test-BarkRanger-2026.09.13_15-45-32--0400.xcresult`. Activity/expedition Swift formatting/lint passed. Local activity persistence, offline command/recovery ownership, recorder hookup and all current-screen/account-lifecycle integration are still outstanding.

### Source review and recovery checkpoint (September 13, after owner status request)

- The 15:57 real-SDK/local run passed 24 iOS checks with no failures/skips. Evidence: `/Users/carterswarm/Library/Developer/Xcode/DerivedData/BarkRanger-dnckgpoploburjgmiagvhrgvfsyf/Logs/Test/Test-BarkRanger-2026.09.13_15-57-00--0400.xcresult`. It covered new activity rows, history/cursor persistence, reopen, selected-state/completion caches and some late-response paths. It did **not** establish general reconciliation correctness.
- Subsequent source review found a concrete new defect in `NativeStore+ActivityReconciliation.swift`: a newer individual activity read can raise the global history floor above a fixed-upper change scan. The code then skips that scan's records while still advancing the durable cursor. An unrelated activity update/deletion in the skipped interval may never be applied, leaving stale cached data. The next inclusive scan does not recover an interval already advanced past. This contradicts the current code comment and requires repair before integration/acceptance.
- Added typed `NativeActivityDraft` / `NativeExpeditionOperation`, expedition queue/staging/outcome handling, `NativeExpeditionSync`, shared client-time validation and `NativeSyncJobs`. The last app/test-target build succeeded. Their dedicated integration, conflict recovery, coherent pending totals and UI/recording hookup are **not complete**. Do not report them as verified end-to-end.
- Lifecycle remains inconsistent: profile/trip/visit workers still use their own coalescing and terminal transport-close implementation, while the new expedition worker uses the shared job owner. Review shared-job paused-flight retirement before reopening admission; the source review identified a possible completed/cancelled-flight reuse race, not a reproduced passing/failing runtime result.
- Architectural assessment: independent entities, typed identity and server command boundaries are useful improvements. The total codebase is more complex while both graphs coexist. NativeStore plus its extensions currently comprise approximately 3,400 lines; this is a responsibility-review signal, not a numeric failure criterion. Keep one atomic writer, but do not treat smaller files as proof of separation or add more layers just to reduce file sizes.
- Owner requested a few focused repair/implementation prompts. Created five sequential recovery checkpoints: foundation/account → trips/map → visits/walks/progress → Apple/essential services → source review/hardening/native deployment/handoff. Each connected feature must lose its obsolete native runtime path before being called complete. No broad rollback or new infrastructure restart is planned. Only planning/progress documents changed in this planning turn; no application/backend code or cloud resources were changed.

### Outstanding Phase 1 integration and operations

Source-review correction: `PersonalState.selectedTripID` and `LocalStore+TripDays` keep the active trip/day device-local; they do not synchronize a cloud selection. Preserve that behavior. The proposed cloud selection row/command was unnecessary scope and has been removed from the new, undeployed backend. Native selection stays in the account-scoped local transaction, including local deletion/clear handling; no selection sync queue is required. Earlier selection-related verification entries describe the superseded draft implementation, not a deployed feature.

- Account/profile/settings, trips, visits, walks and standings now have native connected paths as described above. Preserve the established clear-before-drain lifecycle. Remove the remaining historical dependency surface and freeze a compatible native schema in checkpoint 4; do not reconnect the removed account-wide publication path.
- Finish catalog, support, deletion, Apple services and the remaining operational contracts. The activity missed-interval defect is repaired in checkpoint 3. Trip raw-codec usage remains confined to explicit earlier-file recovery/historical adapters, not native steady-state editing. Include the one-row private leaderboard cache in account deletion.
- Indexed bookmark region reads now preserve original files without rekeying. Measure dense-map rendering on hardware and resolve any necessary product decision without an unapproved cap or UI change.
- Configure native providers/App Check and scoped deployment identity; deploy only after target and integration gates. Backups/restore, retention and operations remain outstanding. No new backend deployed during the emulator verification above; production deny-all rules remain unchanged.
- Apple activation remains owner-deferred as described above. Real StoreKit verification, signed notifications and product configuration are unfinished, not a feature that can already be completed by uncommenting one line.
- Installed Node 22.23.2 through the existing version manager, with the download checksum verified. The user's default remains Node 24; native verification explicitly selects Node 22 in its own process. The Node 22 run passed eight backend unit checks, ten Firestore integration checks and the selected real-iOS-SDK/local checks. Firebase confirmed `Using node@22 from host`. Firebase CLI still warns that its next major version requires Java 21; Java/default runtime and unrelated dependencies were not upgraded.
