# Native iOS rebuild: five recovery prompts

September 13, 2026. These five sequential work batches replace the earlier expectation that two broad prompts would be sufficient. They do not restart the project or add future product scope. Prompts 1–4 finish the original Phase 1 build/integration contract; Prompt 5 completes Phase 2 hardening and delivery. A prompt is an acceptance checkpoint, not a promise about hours or the number of follow-up messages.

Use one prompt at a time in this task. If its acceptance conditions are not met, continue or repair that checkpoint rather than starting the next. Do not delegate by default. Additional agents or a different model are not substitutes for clear ownership and direct integration review.

## Current position and first decision

Keep the independent-record data model, typed identities, server command boundaries and useful existing native UI/routing/recording code. Do not reset the repository or rebuild everything again. The current tree contains extensive unrelated and earlier owner changes.

Checkpoints 1–3 now connect account/profile, trips/notes, visits/walks/progress and the five-row leaderboard to native stores/commands in the actual app composition. Device bookmarks use indexed regional reads while preserving original files. Native accounts and guests no longer open or publish the old account-wide graph. The activity missed-change defect and a remotely edited visit's missing-history-row defect are repaired. Local/emulator acceptance, measured costs and remaining limitations are recorded in the execution record. The next implementation checkpoint is **Prompt 4**. None of this establishes a deployed, completed native app or 100K-user readiness.

Owner follow-up: at-a-glance metadata should be cheap and cached, with more expensive content loaded only when needed. The [September 13 current-source review](../reports/ios-native/SUMMARY_FIRST_SOURCE_REVIEW_2026_09_13.md) identifies automatic selected-trip detail reads, whole trip-metadata bootstrap scans, closed-history reconciliation and whole-trip work for a note-only Save. Checkpoint 4 now starts with the cost gate below. This is a refinement inside the two remaining checkpoints, not another rewrite or a new future-feature phase. The source review and plan update did not implement those fixes or complete checkpoint 4.

Owner sequencing update: connect one real feature, verify it, remove its superseded iOS path, then simplify. Fix known correctness prerequisites before the affected feature is used. The walk reconciliation defect is a walk-slice gate, not a reason to block account/profile or trips. Do not undertake another broad, isolated framework-building pass.

## Rules carried by every prompt

- Read `AGENTS.md`, `04-docs/FIREBASE_PROJECT_OWNERSHIP.md`, the main rebuild plan, this document and the latest execution record. Current source takes precedence over stale progress claims. Do not start over or repeat completed infrastructure setup.
- Use only the approved native project `bark-ranger-ios` and isolated emulator `demo-bark-native`. Native cloud changes require explicit native configuration and target guards; scope deployment credentials before deployment. Keep existing projects, users, web app, backend and secrets untouched. Their retirement is owner-led after acceptance.
- Preserve the current UI, five leaderboard rows and personal standing, navigation, feature limits, scoring, notes/duplicate semantics, guest planning, export and sharing. Do not introduce product changes to make implementation easier.
- Journal, multiple dogs, repeat visits, photos and permanent trail archives remain concise roadmap comments at real boundaries, not new features, unused collections or speculative frameworks.
- No broad rollback, destructive cleanup, account-wide replacement object, mirrored old/new feature authorities, microservice expansion, protocol-per-method scaffolding or cosmetic file splitting. One local transaction writer remains valid; small files alone are not evidence of separation.
- Use source tracing and real connected behavior as acceptance evidence. Write targeted checks for identified failure mechanisms and exercise the relevant app flow, but never substitute passing counts for ownership, complexity or performance analysis.
- Keep confirmed data, drafts and pending intents distinct. Do not evict authored work, silently drop uncertain commands, reassign a recording or interpret unavailable data as empty. Report required conflict choices honestly.
- Preserve the approved $25 monthly native budget alerts and $10 development/load-test ceiling. Alerts are not a hard cap. No additional billing/legal commitments, public release or large cloud load run without required authority.
- Update `04-docs/operations/IOS_NATIVE_REBUILD_PROGRESS.md` with connected paths, removed paths, source findings, verification, resource observations and remaining limitations. Say incomplete when a gate is incomplete. At the end of each prompt, report its outcome and stop for the next prompt.

## Prompt 1 — Connect account/profile and repair its actual dependencies

Completed for local/emulator acceptance on September 13 at 17:44; see the execution record for evidence and remaining deployment/provider gates. Prompt 2 has subsequently completed locally as recorded below. Do not restart this slice or claim whole-app completion.

Implement recovery checkpoint 1 in `04-docs/plans/IOS_NATIVE_REBUILD_RECOVERY_PROMPTS_2026-09-13.md`, following all of its shared rules. This is a focused repair and connected account/profile slice, not another general rewrite.

1. Scope this feature to authentication, profile display/name, account map appearance, access display, explicit profile conflict handling and safe account switching. Purchases/account deletion remain Prompt 4. Do not connect the new database through the old whole-account profile representation.
2. Give this connected profile path safe coalescing, reusable pause, cancellation/drain, resume and terminal scope close. Keep feature conflict/dependency rules separate. If using `NativeSyncJobs`, correct and verify its pending-flight retirement before reopening admission. Do not force unrelated workers into a common framework in this checkpoint.
3. Introduce and wire the small native account-scope owner. Connect existing authentication/profile/settings/access presentation to native repositories and persistence in the actual app composition. Clear old visible state immediately on scope change, close admission, retain/checkpoint/drain the old scope, then open and publish the new one. Preserve guest isolation and recoverable local work. Apple-dependent sign-in activation stays off while approval is pending.
4. Review `NativeStore` responsibilities while touching these paths. Keep atomic storage together; extract only demonstrably misplaced pure rules or duplicate mechanics. Do not create an actor per feature or split files solely to meet a size target. Do not expand into unfinished trip/visit/walk presentation work in this checkpoint.

Acceptance: the real current account/profile flow uses the native path and survives offline/relaunch/account change, with source review and targeted fault/SDK/UI checks. No old account-wide data object is introduced into that completed path. Other features may remain unfinished, but must not call legacy services using the new Firebase registration. Identify remaining temporary local feature adapters precisely; they are not production fallbacks. No live deployment in this checkpoint. Stop and report after this first feature; do not continue automatically to trips.

## Prompt 2 — Connect trips, notes and map storage; remove their old path

Completed for local/emulator acceptance on September 13 at 19:31. The final source-reviewed run passed 89 app/UI test definitions with no failures or skips; resource measurements and remaining limitations are in the execution record. The native trip runtime has no account-wide graph/legacy transport; one-time earlier-guest decoding and DEBUG-only historical regression adapters are explicitly retained for recovery/testing, not as production fallbacks. Next is Prompt 3. Do not repeat this slice, deploy it independently or claim whole-app acceptance.

Implement recovery checkpoint 2 in the recovery-prompts document, following its shared rules. First verify Prompt 1's acceptance record; finish unresolved dependencies before proceeding.

1. Connect the existing Map and Planner to the native trip repository/store through exactly one active-trip decision-maker and editable buffer. Opening, editing, day selection, saving, duplicating, clearing and deleting must follow the real UI path, not a standalone factory.
2. Preserve local checkpoint, save/cancel, note ownership, duplicate and conflict semantics. Use the selected version's preimage; late acknowledgments cannot overwrite newer typing or another open trip. Handle remotely deleted trips explicitly. Keep active trip/day selection device-local.
3. Replace whole-library arrays and raw record decoding in these production consumers with small metadata pages and selected details. Remove the temporary trip codec/raw `expected` bridge from the native runtime after the consumers are converted. Do not add another projection that reconstructs the same whole-account graph.
4. Connect the existing device saved-place/map path to indexed regional queries. Preserve existing bookmark files and stable identities; do not silently merge, upload or delete them. Keep current presentation and routing geometry reuse. If a measured dense-view limit requires a product choice, report it rather than hiding arbitrary pins.
5. Inspect the actual edit → checkpoint → durable operation → server result → visible update path. Record touched rows/encoded bytes and irrelevant subscriber or directions work for representative changes.

Acceptance: current trip/map workflows operate through the native system, including offline edits, interrupted save, conflicting remote edits, cache pressure and reopen. Existing notes, bookmarks and route presentation are preserved. The completed trip path no longer relies on old account-wide arrays or legacy cloud transport. Any leftover trip adapter must be justified and removed before calling this checkpoint complete.

## Prompt 3 — Connect visits, walks, progress and leaderboard

Completed for local/emulator acceptance on September 13 at 21:07; see the execution record for source findings, connected screen/SDK checks, resource evidence and remaining physical/deployment gates. Do not restart this slice or restore its removed native account-graph path.

Implement recovery checkpoint 3 in the recovery-prompts document, following its shared rules. Build on the accepted account/trip paths, not a second account lifecycle.

1. Connect existing map visit actions, Passport, visit history/date changes/bulk removal, daily activity and progress to native commands and small projections. Keep current one-active-visit-per-site and reward rules; repeat-visit UI remains future work. Expose explicit conflict decisions with preserved authored values.
   Before connecting walk synchronization, correct its confirmed skipped-interval defect: an individual read of activity A must not cause activity B's update/deletion to be skipped while advancing the change cursor. Preserve bounded cache, stale-response protection and atomic cursor commits, and verify multi-entity delayed responses.
2. Finish and integrate the activity queue, activity corrections/removal, virtual-run assignment/completion, history paging and latest-completion display. Preserve independent history edits and immutable recording identity/measurement/run attribution. Do not let rejected or uncertain unrelated operations become accidental global feature locks.
3. Connect recorder finish/recovery and existing explicit Health import. Durable handoff must precede allowed temporary-recording cleanup. Repeated recovery/import cannot double-count or discard the original summary. Never silently move an activity to a newly selected run. Preserve the current raw-track lifecycle and future archive handoff comments; do not implement permanent trail archives yet.
4. Define and implement coherent confirmed-versus-pending totals. A lost server acknowledgment followed by a newer state read must not count a pending walk twice. Do not treat optimistic revisions or timestamps as server-confirmed. Resolve this in a small explicit projection contract, not a rebuilt expedition/history blob.
5. Complete native five-row leaderboard and personal standing using current scoring/tie semantics and bounded reads. Preserve current sharing/export. Remove obsolete visit/expedition/profile-array adapters from the native production graph.

Acceptance: exercise these existing screens through the native system, including unrelated-operation conflicts, duplicate import, interrupted recording handoff, offline correction/removal, account switch, stale history/deletion and unchanged reward rules. Recording source is retained on failed handoff. No growing account history/duplicate-ID array or old account-wide feature publication remains in the connected native path. Synthetic location and emulator success must not be reported as physical outdoor/Health acceptance.

## Prompt 4 — Finish Apple implementation and essential native services

Owner sequencing refinement (September 14): first finish and report the local **correctness/coupling checkpoint**—stale/deleted trips, exact detail-cache certification and targeted library invalidations—then stop for the next tranche. Its implementation/verification is recorded in the [execution record](../operations/IOS_NATIVE_REBUILD_PROGRESS.md). Next reduce redundant detail downloads and implement coherent note-only saves. These are ordered parts of this checkpoint, not additional rewrites or future product phases. Do not claim the cost gate below is complete merely because its correctness prerequisites pass.

September 14 follow-up: the owner authorized that next slice after requiring reproduction. Redundant worker preflight and independent library-owned detail reads are removed; existing stop-note-only Save is now one atomic batch with correct unchanged-itinerary acknowledgment/dependency handling. Local/SDK/UI acceptance and full-operation measurements are in the execution record. This does **not** complete the cost gate: first-note/day-note saves, canonical confirmation downloads, archive bootstrap and closed activity-history work remain explicit limitations, alongside the service work below.

Implement recovery checkpoint 4 in the recovery-prompts document, following its shared rules. Verify that the connected features from Prompts 1–3 remain the sole native runtime paths.

**Cost gate before service completion:** follow the concrete source findings and acceptance matrix in the [summary-first review](../reports/ios-native/SUMMARY_FIRST_SOURCE_REVIEW_2026_09_13.md). Separate overview freshness, active detail/history demand and durable command delivery at the existing owners. Reuse cached details without unconditional foreground reads; do not mistake unchanged itinerary revision for unchanged notes. Replace archive-wide reconstructible trip-cache bootstrap with bounded requested-data recovery that preserves dirty work, newer acknowledgments and deletion semantics. Make note-only explicit Save proportional to changed notes while retaining coherent multi-note Save/Cancel. Count read-admission writes and complete pagination workload, not merely callable counts/page limits. Decide whether packed official-site flags are worth their extra writes from measured usage; do not build them speculatively. Current pin selection stays local and the current UI stays intact. Explicit offline-download/clustering UI and journal/dog/media implementations remain separately gated future work.

1. Implement actual StoreKit purchase/restore/current-access handling and backend signed-transaction verification, account ownership, idempotent/out-of-order notifications, renewals/refunds/revocation and reconciliation. Do not substitute comments or mocks for the implementation. Verify current official Apple contracts. Do not invent product IDs, prices, team IDs or agreements.
2. Leave only genuinely Apple-approval-dependent activation disabled at explicit configuration points. Non-Apple workflows must remain testable in a clearly isolated local development setup; no live/public fake Premium grant or release bypass. Record each remaining provider/device gate and its impact. Approval alone is not evidence that sandbox/device flows passed.
3. Complete independent native catalog publication/delivery, support, account management/deletion and privacy-safe diagnostics. Account deletion must block stale-token/bootstrap recreation, stop new writes, and resumably remove its private data and deduplication/interval evidence, including the private `leaderboardCache` introduced in checkpoint 3. Never assume deleting a parent document deletes its children.
4. Finish native auth/provider and App Check configuration, exact-target deployment credentials/guards, indexes/retention, backup/restore procedure, monitoring and budget visibility. Do not add excluded legacy services or copy their secrets. Review any recurring cost against existing authority.
5. Remove remaining old account-wide iOS storage/sync/transport dependencies from the shipping native composition. Freeze a compatible native schema/migration contract before handing it user data. Keep unrelated repository/web code intact.

Acceptance: all current feature implementations and essential service contracts exist and are connected; no core placeholder or temporary legacy bridge remains. Produce an exact ready/disabled/externally-blocked activation inventory and a reviewed deployment manifest. An unavailable Apple provider gate must not stop unrelated work, but must not be relabeled as completed integration. Do not perform public submission or retirement. Final live deployment and installable owner handoff belong to Prompt 5.

## Prompt 5 — Source review, hardening, native deployment and owner handoff

Implement recovery checkpoint 5 in the recovery-prompts document, following its shared rules. Do not treat earlier acceptance claims or test totals as proof. Re-read the connected source.

1. Trace account change, note edit, trip save, visit and recording finish from current screens to authoritative persistence/server results. Confirm one editing/lifecycle owner, bounded feature reads, meaningful local transaction boundaries and no recreated whole-account graph. Review both newly added and remaining code for unnecessary indirection and duplicated ownership. Fix findings and recheck affected paths.
2. Run relevant rules, integration and regression checks plus actual UI workflows. Exercise lost replies, conflicting edits, offline/reconnect, process death, full storage, cache eviction, schema upgrade, deletion and native backup restore. Correct engineering defects before asking the owner to accept them.
3. Measure representative launch, memory, touched rows, encoded bytes, listeners, cloud reads/writes, command latency, large archives, dense local maps and 100K-entry leaderboard workloads within the approved isolated environment and spending ceiling. Include the summary-first acceptance matrix: tap-caused zero Firestore operations for cached pins, no unseen detail/archive bootstrap on overview-only use, cached detail reuse, remote note-only invalidation, closed-screen cancellation without stopping authored sync, and proportional note-only Save. Inspect contention and per-user/per-action costs. Label estimates, simulator measurements and real-cloud/device results separately; do not promise capacity from user count alone.
4. Deploy only the reviewed native manifest using native-scoped credentials and explicit `firebase.native.json --project bark-ranger-ios`; verify destinations, rules, indexes, handlers and operational configuration after deployment. No broad root-config deployment and no non-target changes. Exercise the deployed system using approved development/acceptance accounts.
5. Exercise available Apple sandbox/device and real-device workflows. Identify unavailable external/physical checks without fabricating success. Provide a versioned installable owner-testing build, exact native environment and short hands-on checklist. A core unresolved data-loss, account-isolation, purchase-integrity or architectural defect blocks completion. Apple activation still awaiting approval must be described as a specific disabled limitation, not a finished purchase-ready release.

Acceptance: a source-reviewed, connected native build is genuinely available for owner testing, with recorded resource findings, resolved engineering failures and explicit external limitations. Give the owner a concise completed/not-completed report. Owner acceptance and the separate owner-led retirement of the old system follow; neither is silently performed by this prompt.
