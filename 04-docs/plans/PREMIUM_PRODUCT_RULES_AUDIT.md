# Premium Product Rules Audit

Date: 2026-05-03
Scope: New premium/internal app only.
Status: R05-R12 audit completed; BUG-017 QC PASSED after focused product-rule smoke. BUG-022 settings/cloud-sync policy QC PASSED.

## Audit Table

| ID | Rule | Expected Free Behavior | Expected Premium Behavior | UI Gated? | Runtime Gated? | Backend/Rules Gated? | Test Coverage | Status |
|---|---|---|---|---|---|---|---|---|
| R01 | Free users can mark up to 5 parks visited. | Can add visits until exactly 5 official parks are visited; adding the 6th is blocked with clear copy. | Can exceed 5 visited parks. | Yes: panel shows a clear limit message when a free add is blocked. | Yes: `checkinService` blocks manual and GPS additions at 5 for signed-in non-premium users. | Yes: `firestore.rules` denies non-premium direct `visitedPlaces` writes above 5 without extra rules reads. | `bug015-free-visited-limit-smoke.spec.js` covers free 4->5 allowed, free 5->6 blocked, fake localStorage premium bypass blocked, removal at limit allowed, and GPS blocked at 5. Rules tests cover direct Firestore writes. | BUG-015 QC PASSED |
| R02 | Premium users can mark more than 5 parks visited. | Not applicable. | Can add the 6th and beyond. | Not blocked. | Yes: `premiumService.isPremium()` bypasses the free limit only for the current entitled user. | Yes: active/manual-active entitlement on `users/{uid}` lets premium users write beyond 5. | `bug015-free-visited-limit-smoke.spec.js` covers premium 5->6 allowed and a following free context re-applying the limit; rules tests cover premium direct write beyond 5. | BUG-015 QC PASSED |
| R03 | Free users cannot use premium route generation / ORS route tools. | Route generation appears upgrade-gated, tapping it explains Premium routing, and click/action should not call ORS. | Can route through premium flow. | Yes: `#start-route-btn` is `aria-disabled`, labeled `Premium Route`, styled as locked, and opens a route-specific upgrade prompt. | Yes: click/direct runtime path checks `premiumService.isPremium()` before `services.ors.directions()`. | Yes for direct callable attempts: backend callable rejects non-premium before ORS transport. | `bug016-route-generation-gating-smoke.spec.js`, `bug021-route-upgrade-prompt-smoke.spec.js`, function tests, callable emulator tests. | BUG-016/BUG-021 QC PASSED |
| R04 | Premium users can use premium route generation. | Not applicable. | Route generation works through premium callable/backend path. | Yes: button remains enabled as `Generate Route`. | Yes: premium route generation reaches the ORS directions service path. | Yes: existing ORS callable allows active/manual_active. | `bug016-route-generation-gating-smoke.spec.js`, function tests, callable emulator tests. | BUG-016 QC PASSED |
| R05 | Free users cannot use global town/city search if it spends ORS geocode quota. | Global search remains upgrade-gated and no ORS geocode request is made, even with fake storage premium flags. | Can use global search. | Yes | Yes: `searchEngine` checks `premiumService.isPremium()` before `services.ors.geocode()`. | Backend callable rejects free users before ORS transport. | `bug017-product-rules-audit-smoke.spec.js`, existing premium-gating smoke, function tests, callable emulator tests. | BUG-017 QC PASSED |
| R06 | Premium users can use global town/city search. | Not applicable. | Global town/city search reaches the ORS geocode service path. | Yes | Yes | Backend callable allows active/manual_active. | `bug017-product-rules-audit-smoke.spec.js`, function tests, callable emulator tests. | BUG-017 QC PASSED |
| R07 | Free users cannot enable premium clustering. | Toggle disabled and runtime setting forced false, including forced DOM change and fake storage attempts. | Can enable clustering. | Yes | Yes | Cloud settings payload sanitized client-side; no backend count/gate. | `bug017-product-rules-audit-smoke.spec.js`, existing premium-gating smoke. | BUG-017 QC PASSED |
| R08 | Premium users can enable premium clustering. | Not applicable. | Toggle/setting works and can be reset. | Yes | Yes | No backend-only enforcement for UI preference. | `bug017-product-rules-audit-smoke.spec.js`, existing premium-gating smoke. | BUG-017 QC PASSED |
| R09 | Free users cannot use premium map styles / premium visited filters. | Premium styles/filters reset to free-safe defaults and forced DOM/storage changes are rejected. | Can use premium styles/filters. | Yes | Yes: `mapEngine` and `uiController` now coerce premium-only values through `authPremiumUi`. | Cloud settings payload sanitized client-side; no backend-only enforcement for UI preference. | `bug017-product-rules-audit-smoke.spec.js`, existing premium-gating smoke. | BUG-020 QC PASSED |
| R10 | Premium users can use premium map styles / visited filters. | Not applicable. | Premium styles/filters work and can be reset. | Yes | Yes | No backend-only enforcement for UI preference. | `bug017-product-rules-audit-smoke.spec.js`, existing premium-gating smoke. | BUG-017 QC PASSED |
| R11 | Free users cannot use virtual/completed trail premium controls if those are paid. | Controls locked and forced click/action relocks without toggling overlays. | Can toggle overlays. | Yes | Yes: `expeditionEngine` checks `premiumService.isPremium()` before toggling/fly-to behavior. | Not applicable unless backed by callable. | `bug017-product-rules-audit-smoke.spec.js`, existing premium-gating smoke. | BUG-017 QC PASSED |
| R12 | Premium users can use virtual/completed trail controls. | Not applicable. | Controls are enabled and toggle active state. | Yes | Yes | Not applicable unless backed by callable. | `bug017-product-rules-audit-smoke.spec.js`, existing entitlement smoke. | BUG-017 QC PASSED |
| R13 | Checkout success URL never unlocks premium. | Success URL shows safe verification/sign-in state and never grants premium. | Premium active appears only after verified entitlement. | Yes | Yes | Firestore entitlement is source of truth. | Existing premium-gating smoke. | QC PASSED |
| R14 | Firestore/client cannot self-write entitlement. | Client writes to entitlement/payment/admin fields denied. | Same; entitlement changes must come from backend/admin. | Not a UI rule | Not a runtime client grant | Firestore rules enforce. | Rules tests 17/17. | QC PASSED |
| R15 | ORS callable rejects free users server-side. | Direct callable attempts fail before ORS transport. | Active/manual_active reaches ORS transport. | Not sufficient alone | Yes | Callable enforces entitlement server-side. | Function unit tests and callable emulator tests. | QC PASSED |
| R16 | Cloud settings sync is Premium while local settings autosave is available to everyone. | Local settings persist on the device; Save to Cloud/Cloud Sync shows Premium copy and does not write Firestore; premium-only settings are sanitized. | Local settings persist and cloud settings sync can save premium settings to Firestore. | Yes: cloud sync copy/button says Premium for free users without implying local settings require Premium. | Yes: save/autosave/hydration check `premiumService.isPremium()`. | Firestore settings writes are only attempted by the premium client path; full hardening could move cloud sync behind a callable later. | `bug022-settings-cloud-sync-policy-smoke.spec.js` covers signed-out local persistence, signed-in free local-only/no cloud write/upgrade prompt, and premium cloud sync payload. | BUG-022 QC PASSED |
| R17 | Saved route save/load is Premium while manual trip planning remains free. | Can manually plan stops, but save/load/list saved routes opens Premium copy and does not call saved-route Firestore reads/writes. | Can save, list, and load saved routes. | Yes: save/load surfaces open saved-route Premium paywall copy. | Yes: trip planner and route renderer check `premiumService.isPremium()` before saved-route reads/writes. | Yes: `firestore.rules` require premium entitlement for saved-route read/create/update; owner delete is allowed for cleanup. | `bug026-trip-save-custom-stop-smoke.spec.js` covers free paywall and premium save/load; route-renderer unit tests cover no free read; rules tests cover direct Firestore denial. | QC PASSED |

## Notes

- This audit is opened because the previous beta gate covered auth/payment/security better than product-tier rules.
- BUG-015 is now client/runtime QC passed, with a documented backend hard-quota gap if visited writes move behind a callable later.
- BUG-016 is now UI/runtime/backend QC passed.
- BUG-021 improves the route-gating UX without broadening access: the route action remains blocked for free users, but now opens an explanatory Premium upgrade path on tap/click.
- BUG-022 keeps basic local settings autosave available to everyone while making cloud settings sync a Premium feature. Free users no longer write or hydrate Firestore cloud settings, and premium-only settings remain sanitized.
- R17 closes the saved-routes loophole: free users can still plan manually, but persistent save/load is premium-only in UI, runtime, and rules.
- BUG-017 R05-R12 audit found BUG-020: premium map style/visited filter handlers trusted disabled controls. The fix adds runtime coercion and free-safe cloud payload serialization; focused audit smoke passes for free and premium accounts.
- Backend/rules enforcement may not be feasible for every UI preference, but any quota/cost/data product rule must have more than a visual-only gate.
