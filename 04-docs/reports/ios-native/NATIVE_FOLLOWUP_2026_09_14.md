# Native follow-up evidence — September 14, 2026

Scope: iOS and isolated native backend only. Starting checkpoint: `427b268`.
Unrelated pre-existing working-tree changes are excluded from every commit.

## 1 — Contract drift and CI

- Reproduction before the fix: the new shared fixture exposed 16 invalid inputs accepted by Swift. These included invalid IDs, oversized place labels, malformed dates/times, colors and visit durations. The JavaScript validator already rejected them.
- Fix: validate these fields in the existing `Trip.validate` boundary before saving an outbound intent. Use one checked-in JSON fixture from both real Swift and JavaScript command constructors, not separate test-only validators. Preserve the server's Gregorian calendar contract, including ISO year 0000.
- CI: replace the retired iOS emulator/config/seed with `firebase.native.json`, `demo-bark-native`, and the native profile, trip and adventure groups. Both workflows react to shared domain/fixture changes. A regression check protects the target selection.
- Checks: domain **47 passed** (one test contains 36 shared examples); backend unit **48 passed**; native backend integration/transport **28 passed**; CI/project isolation **4 passed**; both workflow files parse successfully. App ordinary suite **279 passed, 27 opt-in checks skipped**; those skips are not claimed as cloud-integration passes.
- Runtime size: iOS + domain Swift **25,701 → 25,736 lines (+35)**, **296 → 296 files**. Backend runtime unchanged. This checkpoint corrects validation; it is not presented as a code-reduction refactor.
- Simulator native trip/account acceptance: **94 passed, zero failures/skips**, using the real SDK, isolated emulators and current UI. Includes save, relaunch, pending changes, account isolation and offline-store cases. Evidence: `/var/folders/71/0jrgj85x78g562jhy30l4j600000gp/T/BarkAccountChecks-1l528t2u/Acceptance.xcresult`. This is not physical-device or production latency acceptance.

## 2 — Deletion lifecycle

- Implemented recent-auth (five-minute), explicitly confirmed native account deletion. Free accounts can delete without Premium. The app drains writers, removes the exact account's local records/pins/recording/draft files, signs out and retains an accepted cleanup marker across interrupted local cleanup. Other owners' authored data and guest drafts remain separate. In-memory failed-draft recovery is explicitly forgotten for the deleted scope.
- Added a private, resumable scheduled worker. It deletes Auth, the complete owner subtree (including nested itinerary documents), the hashed leaderboard entry and UID-indexed receipt pages of 200. Pending jobs never expire. Completed jobs retain only a two-day UID fence against still-valid old ID tokens. Bootstrap checks this fence; ordinary commands do not pay for it.
- Closed the delayed leaderboard-cache write race with deletion. A cache miss adds one profile read in its final write transaction; a cached rank does not. No extra deletion-fence read is added to ordinary trip/visit/note commands.
- Detached trip-owned planning notes retain text for 90-day offline recovery, then TTL removes them. Relinking replaces the record without expiry. Independently owned places and future journal records do not use this trip-note policy.
- Native deployment succeeded via the scoped deployer. Four functions are ACTIVE in us-east1. New note/job TTL policies are ACTIVE, the receipt UID index is READY, and the cleanup schedule is ENABLED. Runtime gets only `firebaseauth.users.delete`, not broad Auth administration. No old project, users, hosting or web code changed.
- Verified **80 backend checks passed**, including interruption after Auth deletion, 405 paged receipts, unrelated-owner preservation, stale-token bootstrap, delayed cache writes and note relinking/expiry. App ordinary suite **283 passed / 27 opt-in skipped** before the additional recovery-buffer check; final focused deletion/recovery checks **6 passed**. Corrected native profile/UI acceptance **40 passed / zero failures or skips**, including cancel, deletion and relaunch. The initial UI check exposed a missing visible Cancel button; a standard confirmation alert fixed it. Final UI evidence: `/var/folders/71/0jrgj85x78g562jhy30l4j600000gp/T/BarkAccountChecks-a4di7pv9/Acceptance.xcresult`.
- Broader native union: **174 passed / 2 failed**. Both leaderboard failures traced to five malformed synthetic IDs left by an earlier deletion test in the shared emulator. The deletion test now owns a fresh named emulator database and valid hashed identities. Removed only those five verified synthetic rows; both failed checks then **passed** without app/backend changes (`BarkAccountChecks-moketer3/Acceptance.xcresult`). Do not call this a single 176-pass run.
- CI follow-up: the runner now unions all three native groups without rerunning shared suites. Included the pre-existing single-line catalog `polling` fixture fix after reproducing missing endpoint **404 → 200**. Native checks run before the broad screen suite; no checks are disabled. Local CI/project guards **7 passed**. The first pushed backend GitHub workflow passed; its iOS workflow hit its time limit and also reported broad shell/accessibility/deep-link failures outside this checkpoint. Those failures are not represented as fixed or green CI.
- Real-cloud disposable QA passed sign-up/save/note/replay and the complete deletion lifecycle: private worker, Auth removal, no nested records or receipts, and old-token resurrection denied. Temporary QA credentials/debug registration were removed. This did not delete the owner account. TTL configuration is verified; a natural 90-day expiry has not been waited out.
- Size: iOS runtime **25,736 → 25,941 lines (+205)**; backend runtime **2,204 → 2,334 lines (+130)**. This is a missing deletion capability, not a claimed code-size reduction. Device route files historically hash owner+route together, so deletion clears the disposable road-geometry cache rather than pretending it can identify that owner's files. Authored trips are not that cache.
- Operational limit: one worker, up to twenty accounts per invocation every fifteen minutes, with a seven-minute loop budget and resumable failures. This is bounded initial capacity, not a measured deletion-throughput guarantee at 100K users. See [Firebase scheduling](https://firebase.google.com/docs/functions/schedule-functions) for the schedule's small fixed charge; the existing development spending envelope still applies.

## 3 — Saved-pin synchronization

- Account pins now use the existing private `users/{uid}/places/{placeID}` and the same durable NativeStore outbox, mailroom and foreground scheduler. No new cloud collection, alternate retry engine or Premium requirement. Explicit membership is separate from trip references; saving a trip does not star all its stops. Guest pins remain local and are never silently uploaded into an account.
- A new pin remains light yellow / “Saved on iPhone” until its exact operation is acknowledged. Pending Changes includes save/removal descriptions and safe never-sent Discard. Replays cannot resurrect a later remote removal. Un-saving never deletes places, trip notes or future journal content. The tiny false membership remains for incremental offline reconciliation and is removed by account deletion; this is intentional, not an accumulating receipt/event collection.
- The native spatial index is a rebuildable projection. With **300 cached pins**, the first build projected **300 rows**, **100 repeated map queries projected 0**, and one remote removal projected **1**. Real SDK checks also recorded **zero function calls for 100 browse/select repetitions**. Fresh-device discovery uses bounded metadata pages and a durable exact server cursor; there is no listener/query attached to zooming or tapping.
- Instrumented first save: **5 document reads, 3 writes, 1 function call**, including receipt/profile/entitlement/rate admission. Serialized synthetic confirmation: **489 bytes**. Same-command replay: **1 read, 0 writes**. Confirmation requires no follow-up detail call. Checkpoint 4 will remove unnecessary admission work; these are checkpoint-3 counts, not final targets or observed billing invoices.
- Exactly-account-owned prelaunch pin files move into canonical rows plus queued operations atomically, then their authored JSON files retire. Local text is retained but not uploaded with map metadata. Wrong writer/owner pairs are rejected. Initial verification caught a stale legacy index after retirement; marking that projection dirty before removing its source file fixed it.
- Verification: **97 backend checks**, **48 domain checks** (including 14 shared saved-pin examples), **19 focused simulator checks, zero failures/skips**; includes real account-model switching, offline pending recovery, store upgrade without resetting existing pending bytes, yellow/confirmed rendering, relaunch and the existing saved-pin UI. Final ordinary app suite: **290 passed, 29 opt-in skipped**. Local CI/project guards: **7 passed**.
- Cloud deployment is native-only. The new place index reached **READY**. First acceptance encountered the still-building index, not a successful query; that disposable account was deleted. A fresh real-cloud run then passed free save/confirmation/change-page/removal/replay, shared place identity with a trip, and preserved trip notes. Its disposable account/debug registration were also removed through the verified deletion lifecycle. Owner data was not deleted.
- Runtime size: iOS **25,941 → 26,722 lines (+781), 298 → 305 files**; backend **2,334 → 2,394 lines (+60), 52 → 53 files**. This adds synchronization, local transactional/index projection and UI integration; it is not a code-reduction claim. No journal/media UI or old-web work was added.

## Remaining ordered work

4. Reduce avoidable save/read work; measure identical actions before/after.
5. Remove repeated queue-wide trip validation from the hot path.
6. Leaderboard measurement at 1K/10K/100K; measurement only, no speculative rewrite.
